<h1>desde websockets</h1>
<div>
    {{#each products}}
    <p>{{this.name}}</p>
    <p>{{this.description}}</p>
    <p>{{this.price}}</p>
    <p>{{this.category}}</p>
    
{{/each}}
</div>

<script src="{{socket}}" ></script>
<script src="{{index}}" ></script>